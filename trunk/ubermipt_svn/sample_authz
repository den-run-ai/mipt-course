# Права доступа в SVN наследуются по дереву,
# поэтому доступ к корневой папке должен быть
# только у преподавателя - на чтение-запись
# (и он автоматически даст доступ к остальным папкам)
# и возможно у пользователя redmine - на чтение
# (если будет нужен веб-доступ к репозиторию):
[/]
teacher = rw
redmine = r

# Доступ к материалам семинаров - у всех на чтение:
[/seminars]
* = r
# Предыдущая строка отменяет доступ преподавателю, восстанавливаем:
teacher = rw

# Каждому студенту надо дать доступ к личной папке:
# [/ivanov]
# ivanov = rw
# Не забудьте создать студенту личную папку!
# Для этого в svn-клиенте выполните:
#   mkdir ivanov
#   sudo add ivanov
#   svn commit ivanov -m "Personal directory for Ivanov"

# Можно создавать общие папки для нескольких
# студентов (например, проект 4го семестра):
# [/supergame]
# ivanov = rw
# petrov = rw
# sidorov = rw
